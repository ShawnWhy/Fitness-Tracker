{
  "name": "fit-ness-tracker",
  "version": "1.0.0",
  "description": "first a javascript file is written to crate a mongo database.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "seed":"node seeders/seed.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "logger": "0.0.1",
    "mongoose": "^5.9.4",
    "morgan": "^1.9.1"
  }
}
